name: CI

# Smoke test: lint + typecheck do dashboard e py_compile dos scripts Python.
# package-lock.json é committado — CI usa `npm ci` para build determinístico.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  web:
    name: Web — install, lint, typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install deps
        run: npm ci --no-audit --no-fund

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

  python:
    name: Python — sintaxe
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Compilar módulos para detectar erros de sintaxe
        run: |
          python -m py_compile ops/importer/import_csv.py
          python -m py_compile ops/indexer/index_fs.py
