name: CI

# Porta de qualidade: lint + typecheck + testes do dashboard e py_compile dos scripts Python.
# package-lock.json é committado, e o CI usa `npm ci` para build determinístico.
#
# ---------------------------------------------------------------------------
# O NODE AQUI É 24 PORQUE 24 É O QUE VAI PARA O SERVIDOR DO ÓRGÃO.
# ---------------------------------------------------------------------------
# É a mesma versão dos três estágios do Dockerfile (ADR-0025) e a que o
# `engines.node` do package.json exige (>=24.0.0).
#
# Até 22/09/2026 este arquivo pedia a 20, e o comentário que ficava aqui afirmava
# que 20 era também a versão da imagem. Era falso desde a ADR-0025: a imagem já
# estava em `node:24-alpine`. O efeito é que a porta de qualidade aprovava o
# código num runtime que ninguém entrega, e o runtime entregue só era exercitado
# por quem rodasse `docker build` na mão.
#
# Não é diferença cosmética. No lock, 20 pacotes declaram `engines.node` que a 20
# não satisfaz, entre eles o `tedious` (driver do SQL Server do órgão, ADR-0023),
# o `@azure/identity`, que é a autenticação dele, e o `@supabase/supabase-js`.
# Como o projeto não tem `engine-strict`, o `npm ci` só avisa e segue: a
# divergência era silenciosa por construção. Somado a isso, a linha 20 saiu de
# suporte em 30/04/2026 (calendário oficial do nodejs/Release), e sistema de
# governo não se valida em runtime que não recebe mais correção de segurança.
#
# Consequência prática: o CI passa a rodar com npm 11, igual à imagem. O
# `npm run lock:ci`, que fixa o npm 10 para gerar o lock, deixa de ser
# obrigatório, porque não sobrou ninguém rodando npm 10. O lock atual, gerado
# pelo npm 10, foi provado em 22/09/2026 com `npm ci` sob npm 11.19.0 dentro do
# `node:24-alpine`, exit 0.
#
# Ao mudar a versão do Node no Dockerfile, mudar aqui também. Não existe guarda
# comparando os dois arquivos, e foi essa ausência que deixou a divergência
# durar: a `tests/unit/infrastructure/proxy-saida-internet.test.ts` cobre só o
# Dockerfile. A versão é escrita literalmente ('24') em vez de derivada do
# `engines`, porque `>=24.0.0` resolveria para a maior versão disponível e
# passaria da imagem no dia em que sair uma linha nova.
#
# Lacuna conhecida em 22/09/2026: nenhum job aqui roda `next build`. Erro que só
# aparece na construção do pacote de produção continua sendo detectado apenas no
# `docker build`, que é manual. Fechar isso custa minuto de Actions e a decisão
# está em aberto.

# A branch de trabalho entra no gatilho de propósito. Ela é onde o
# desenvolvimento acontece desde 19/08/2026, e até 08/09 acumulou cerca de 35
# commits que NUNCA passaram por esta porta: o gatilho só cobria `main`, e a
# fusão para `main` ainda não aconteceu. CI que não roda no lugar onde o código
# é escrito não é porta de qualidade, é enfeite.
#
# É uma branch nomeada e não um curinga porque cada execução gasta minuto de
# Actions em `ubuntu-latest`, e a conta do GitHub é assunto aberto na operação.
# Quando esta branch for fundida, a linha sai junto: o `main` volta a bastar.
on:
  push:
    branches: [main, chore/preparar-container-prodesp-offline]
  pull_request:
    branches: [main]

jobs:
  web:
    name: Web — install, lint, typecheck, testes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install deps
        run: npm ci --no-audit --no-fund

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Testes
        run: npm test

  integracao:
    name: Integração — schema e fluxo transacional em Postgres
    runs-on: ubuntu-latest
    # Prova contra Postgres de verdade o que o mock in-memory não alcança:
    # atomicidade, idempotência sob concorrência, coluna GENERATED, índice único
    # parcial. Exigência escrita da ADR 0021. Também garante que as migrations
    # aplicam do zero e suportam reexecução, que é como o deploy on-prem roda.
    services:
      postgres:
        image: postgis/postgis:16-3.4-alpine
        env:
          POSTGRES_USER: spaguas
          POSTGRES_PASSWORD: spaguas
          POSTGRES_DB: spaguas_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U spaguas -d spaguas_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      TEST_DATABASE_URL: postgresql://spaguas:spaguas@localhost:5432/spaguas_test
      PGPASSWORD: spaguas
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install deps
        run: npm ci --no-audit --no-fund

      - name: Aplicar schema (shim de auth + migrations, do zero)
        run: |
          psql "$TEST_DATABASE_URL" -v ON_ERROR_STOP=1 -q -f db/auth-compat.sql
          for arquivo in $(ls supabase/migrations/*.sql | sort); do
            psql "$TEST_DATABASE_URL" -v ON_ERROR_STOP=1 -q -f "$arquivo"
          done

      # Sem este passo a reaplicação abaixo roda sobre banco VAZIO, e banco vazio
      # faz a migration certa e a errada passarem igual. Foi assim que dois
      # índices transitórios chegaram à produção e derrubaram o serviço
      # (0045 em 10/09/2026, 0057 em 22/09/2026).
      - name: Semear o estado real de produção (a régua sem isto mede o vazio)
        run: psql "$TEST_DATABASE_URL" -v ON_ERROR_STOP=1 -f ops/testing/regua-migrations/semear-estado-de-producao.sql

      - name: Reaplicar tudo (as migrations têm que ser idempotentes)
        run: |
          psql "$TEST_DATABASE_URL" -v ON_ERROR_STOP=1 -q -f db/auth-compat.sql
          for arquivo in $(ls supabase/migrations/*.sql | sort); do
            psql "$TEST_DATABASE_URL" -v ON_ERROR_STOP=1 -q -f "$arquivo"
          done

      - name: Conferir a reaplicação e limpar a semeadura
        run: psql "$TEST_DATABASE_URL" -v ON_ERROR_STOP=1 -f ops/testing/regua-migrations/conferir-reaplicacao.sql

      - name: Testes de integração
        run: npx vitest run tests/integration

      # Este job fica verde com seis arquivos inteiros pulados, porque eles falam
      # com o SQL Server do órgão e isso exige a VPN, que o runner nunca terá.
      # Verde silencioso sobre teste que não rodou é boa notícia falsa: o passo
      # abaixo diz, no log, o que este verde NÃO mediu, e reprova se algum
      # arquivo que fala com o SQL Server esquecer a guarda de skip (sem ela o
      # job quebraria tentando conectar).
      - name: Anunciar o que este verde NAO mediu (precisa da VPN do orgao)
        run: |
          # O `|| true` não é descuido: o Actions roda com `set -e`, e sem ele um
          # grep sem resultado aborta o passo AQUI, antes da mensagem do piso lá
          # embaixo. Reprovaria mudo, e log de CI que reprova sem dizer o motivo
          # é pior que ausente.
          COM_GUARDA=$(grep -lE 'process\.env\.SQLSERVER_HOST.*\? describe : describe\.skip' tests/integration/*.test.ts | sort || true)
          QTD_ARQ=$(printf '%s\n' "$COM_GUARDA" | grep -c '^tests/' || true)
          # `[.(]` em vez de `\(` porque `it.each(...)` também é caso, e a âncora de
          # início de linha é o que impede `.test(p)` de um regex entrar na conta.
          #
          # A contagem é estática de propósito, e é um PISO, não o total: em
          # 23/09/2026 ela dá 67 e o número real é 72, porque um `it.each` gera
          # vários casos. Contar pela ferramenta não é opção: medido no mesmo dia,
          # `vitest list` devolve zero caso para estes arquivos e 3 para todo
          # `tests/integration`, porque ele lista só o que rodaria, e o que eu
          # quero contar é exatamente o que ele pula. O total exato aparece no log
          # do passo anterior, nas linhas `↓ arquivo (N tests | N skipped)`.
          QTD_CASOS=$(grep -hoE '^[[:space:]]*(it|test)[.(]' $COM_GUARDA | wc -l || true)

          # Piso contra leitura vazia. Em 23/09/2026 são 6 arquivos; zero aqui não
          # quer dizer que a dívida acabou, quer dizer que a expressão parou de
          # casar com o código, e daí este passo viraria enfeite verde.
          if [ "$QTD_ARQ" -lt 1 ]; then
            echo "::error title=Regua quebrada::nenhum arquivo casou a guarda de SQLSERVER_HOST. O padrao no codigo mudou: corrigir a expressao deste passo."
            exit 1
          fi

          echo "::notice title=Medicao pendente::$QTD_ARQ arquivo(s) e ao menos $QTD_CASOS caso(s) de integracao NAO rodaram aqui: falam com o SQL Server do orgao e exigem a VPN, que este runner nao tem. Medir na bancada com a VPN ligada."
          echo "Arquivos que este job NAO mediu:"
          printf '%s\n' "$COM_GUARDA"

          # Quem fala com o SQL Server sem a guarda não pula: tenta conectar e
          # derruba o job. Esta metade reprova de verdade.
          FALTANDO=""
          for arquivo in tests/integration/*mssql*.test.ts; do
            [ -e "$arquivo" ] || continue
            if ! printf '%s\n' "$COM_GUARDA" | grep -qxF "$arquivo"; then
              FALTANDO="$FALTANDO $arquivo"
            fi
          done
          if [ -n "$FALTANDO" ]; then
            echo "::error title=Guarda de skip ausente::falam com o SQL Server sem guarda de SQLSERVER_HOST:$FALTANDO"
            exit 1
          fi

  python:
    name: Python — sintaxe
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Compilar módulos para detectar erros de sintaxe
        run: |
          python -m py_compile ops/importer/import_csv.py
          python -m py_compile ops/indexer/index_fs.py

  imagem:
    name: 'Imagem: docker build da entrega offline'
    runs-on: ubuntu-latest
    # Até 23/09/2026 nenhum job construía a imagem, e a entrega ao órgão É uma
    # imagem. Lint, typecheck e 1407 testes ficam verdes sobre um Dockerfile que
    # não constrói, porque nenhum deles olha para ele: quem pega COPY de caminho
    # que deixou de existir, estágio que perdeu dependência de sistema e guarda
    # que mede o artefato é o `docker build` real, e ele só rodava na bancada, na
    # véspera da entrega.
    #
    # O que este job NÃO mede: a imagem não é levantada, então nada aqui prova
    # que o servidor responde. Isso exige Postgres de pé, porque `/api/health`
    # faz SELECT 1, e é prova de outro job.
    steps:
      - uses: actions/checkout@v5

      - name: Construir a imagem como ela é entregue
        # Sem `--build-arg`, e isto não é esquecimento. O Supabase saiu da
        # entrega, os dois `NEXT_PUBLIC_SUPABASE_*` são opcionais tratados por
        # `vazioComoAusente` em src/infrastructure/config/env.ts, e o Dockerfile
        # já carrega `ACESSO_SEM_IDENTIDADE=sim` de fachada para o build. Passar
        # valor de faz de conta seria pior do que não passar: `NEXT_PUBLIC_*` é
        # substituído em tempo de build e fica GRAVADO no pacote, vencendo a
        # variável de ambiente do container em produção.
        run: docker build --progress=plain -t dmo-ci:${{ github.sha }} .

      - name: Provar que a guarda do sharp reprova quando ele está presente
        # Guarda de ausência que nunca reprovou é enfeite verde: ela fica igual
        # se o `find` parar de alcançar o que deveria. O mutante semeia
        # exatamente a cópia que o `outputFileTracingExcludes` não alcança, uma
        # aninhada em node_modules/<pacote>/node_modules/sharp.
        #
        # Ele só custa segundos por causa de uma linha no `.dockerignore`, e isso
        # foi medido na bancada em 23/09/2026: o estágio `builder` faz `COPY . .`,
        # então um arquivo novo no contexto muda o hash da camada, derruba o cache
        # e refaz o `npm run build` inteiro (277,1s e 288,9s em duas execuções).
        # Com `Dockerfile.mutante-sharp` ignorado, os dois builds veem o MESMO
        # contexto e o segundo sai com `COPY . . CACHED`. Quem mexer naquela linha
        # do `.dockerignore` dobra o tempo deste job sem mudar o que ele prova.
        run: |
          ANCORA='RUN test -f /app/server.js'
          grep -qF "$ANCORA" Dockerfile || {
            echo "::error title=Ancora perdida::a primeira linha da guarda do sharp mudou de forma no Dockerfile. Corrigir a ancora deste passo, senao ele deixa de medir."
            exit 1
          }

          # O caminho semeado sai de UMA variável, usada tanto para criar o
          # diretório quanto para procurar a mensagem no log. Isso não é estilo:
          # o texto `SHARP NA IMAGEM` aparece DUAS vezes no log de um build,
          # porque o buildkit imprime o comando do `RUN` antes de executá-lo, e o
          # comando contém o `echo`. Procurar só a frase casa com o comando e
          # aprova qualquer reprovação, inclusive a de âncora quebrada; procurar a
          # frase mais o caminho separa o eco do efeito, porque no comando está
          # `$achado` e só na saída está o caminho resolvido.
          SEMEADO='/app/node_modules/pacote-aninhado/node_modules/sharp'

          awk -v a="$ANCORA" -v s="$SEMEADO" 'index($0, a) == 1 { print "RUN mkdir -p " s } { print }' Dockerfile > Dockerfile.mutante-sharp

          # Uma linha a mais, e exatamente uma. Derivação que não derivou faria o
          # build mutante passar, e o passo reprovaria dizendo que a guarda está
          # inerte quando o problema seria o `awk`: motivo errado não prova nada.
          ESPERADO=$(( $(wc -l < Dockerfile) + 1 ))
          OBTIDO=$(wc -l < Dockerfile.mutante-sharp)
          if [ "$OBTIDO" -ne "$ESPERADO" ]; then
            echo "::error title=Mutante nao foi semeado::esperava $ESPERADO linhas e obtive $OBTIDO. Sem a semeadura o resultado abaixo nao mede a guarda."
            exit 1
          fi

          if docker build --progress=plain -f Dockerfile.mutante-sharp -t dmo-ci-mutante-sharp . > mutante.log 2>&1; then
            echo "::error title=Guarda inerte::o build passou com sharp aninhado dentro da imagem. A guarda do Dockerfile nao esta medindo o artefato."
            tail -30 mutante.log
            exit 1
          fi

          # Reprovar não basta: um build pode falhar por falta de espaço, por
          # rede, por sintaxe ou pela PRÓPRIA âncora da guarda, e eu leria isso
          # como guarda funcionando. Medido na bancada em 23/09/2026 com um
          # segundo mutante que apaga o `server.js`: ele reprova com "GUARDA
          # QUEBRADA", e a busca só pela frase `SHARP NA IMAGEM` casava com o eco
          # do comando e dava verde. Com o caminho semeado, aquele log deixa de
          # casar e o log do mutante legítimo continua casando.
          if ! grep -qF "SHARP NA IMAGEM: $SEMEADO" mutante.log; then
            echo "::error title=Reprovou pelo motivo errado::o build mutante falhou sem imprimir a mensagem da guarda com o caminho semeado. Nao e prova de que ela pegou o sharp."
            tail -30 mutante.log
            exit 1
          fi

          # A evidência vai no VERDE, e não só na falha. Até aqui o log do job
          # guardava a saída do mutante em arquivo e só a imprimia quando dava
          # errado, então o passo verde afirmava por conta própria que a guarda
          # pegou o sharp: quem auditasse o run leria uma frase minha, não a
          # medição. Esta linha imprime o que a guarda disse, com o caminho que
          # ela achou, que é o mesmo que o awk semeou logo acima.
          echo "Linha que a guarda imprimiu dentro do build mutante:"
          grep -m1 -F "SHARP NA IMAGEM: $SEMEADO" mutante.log

          echo "::notice title=Guarda do sharp provada::o build com sharp aninhado reprovou pela mensagem da propria guarda, e o build da entrega passou."

      - name: Provar que nenhuma imagem entregue roda como root
        # Guarda de TEXTO, e nao de build: le os Dockerfiles do disco e exige
        # USER no ESTAGIO FINAL de cada um. Ela existe porque em 23/09/2026 o
        # ops/producao/Dockerfile.migrate era o unico dos cinco sem USER, e a
        # imagem construida rodava como uid=0(root) e no grupo wheel, medido com
        # `docker run --entrypoint sh <imagem> -c id`. O ADR 0015 declarava
        # "containers non-root" como consequencia positiva desde o inicio da
        # conteinerizacao, e nada no repositorio media isso: a declaracao valia
        # mais do que o artefato entregue ao orgao.
        #
        # Por que o USER e procurado depois do ULTIMO FROM: em imagem de varios
        # estagios, USER no `builder` nao muda quem roda a imagem final, e uma
        # guarda que aceitasse qualquer USER ficaria verde sobre o defeito.
        #
        # Por que a lista sai do `git ls-files` e a LEITURA e do disco: a lista
        # nao pode colher o Dockerfile.mutante-sharp que o passo acima gera, e a
        # leitura precisa ver conserto que ainda nao foi commitado.
        #
        # O QUE ESTA GUARDA NAO MEDE: ela nao levanta container nenhum, entao nao
        # prova quem e o processo la dentro. Entrypoint que chama `su`, ou um
        # `user:`/`--user 0` no compose, passariam por ela. Quem prova isso e
        # `docker run --entrypoint sh <imagem> -c id`, e nao esta neste job.
        run: |
          falhou=0
          achados=0
          for arquivo in $(git ls-files '*Dockerfile' '*Dockerfile.*' | grep -v '\.dockerignore$'); do
            achados=$((achados + 1))

            # CRLF existe neste repositorio (o Dockerfile.migrate e CRLF inteiro).
            # Sem tirar o CR, um `USER root` viraria "root\r" e escaparia da
            # comparacao abaixo, aprovando exatamente o que a guarda procura.
            tr -d '\r' < "$arquivo" > /tmp/sem-cr

            # O `|| true` aqui nao mascara defeito, ele PRESERVA a mensagem: o
            # shell deste `run` e `bash -e -o pipefail`, e num arquivo sem FROM o
            # pipeline falharia e o `-e` mataria o passo antes do echo logo
            # abaixo. Medido em 23/09/2026 com um mutante sem FROM: codigo 1 e
            # nenhuma linha dizendo por que, que e o passo vermelho inutil.
            ultimo_from=$(grep -nE '^[[:space:]]*FROM[[:space:]]' /tmp/sem-cr | tail -1 | cut -d: -f1 || true)
            if [ -z "$ultimo_from" ]; then
              echo "::error file=$arquivo,title=Sem FROM::$arquivo nao tem FROM, e esta guarda nao sabe medi-lo. Ajustar a guarda, senao ela deixa de medir este arquivo."
              falhou=1
              continue
            fi

            usuario=$(awk -v n="$ultimo_from" 'NR > n && /^[[:space:]]*USER[[:space:]]+/ { print $2 }' /tmp/sem-cr | tail -1)
            dono=${usuario%%:*}

            if [ -z "$usuario" ]; then
              echo "::error file=$arquivo,title=Imagem roda como root::$arquivo nao declara USER depois do ultimo FROM (linha $ultimo_from), entao o container roda como root no host do orgao."
              falhou=1
            elif [ "$dono" = "root" ] || [ "$dono" = "0" ]; then
              echo "::error file=$arquivo,title=Imagem roda como root::$arquivo declara USER $usuario no estagio final."
              falhou=1
            else
              echo "ok  $arquivo  USER $usuario  (estagio final comeca na linha $ultimo_from)"
            fi
          done

          if [ "$achados" -lt 5 ]; then
            echo "::error title=Guarda sem alcance::esperava ao menos 5 Dockerfiles e encontrei $achados. Se um Dockerfile foi removido de proposito, baixar este piso no mesmo commit; guarda que nao acha arquivo fica verde sem medir nada."
            exit 1
          fi

          [ "$falhou" -eq 0 ] || exit 1
          echo "::notice title=Nenhuma imagem como root::$achados Dockerfiles conferidos, todos com USER sem privilegio no estagio final."
